export class Event {
    catering: boolean;
    date: string; // timestamp instead?
    description: string;
    startTime: string; //timestamp instead?
    endTime: string; // timestamp instead?
    menuItem: string;
    room: string;
    databaseKey: string;
    approved: boolean;
}